# 🚀 Как запустить проект в DataSpell (Пошаговая инструкция)

Привет! Вот подробная инструкция, как открыть и запустить этот проект, используя **JetBrains DataSpell**.

## Шаг 1: Откройте проект 📂
1.  Запустите **DataSpell**.
2.  На экране приветствия нажмите **Open** (или `File > Open` в меню).
3.  Найдите и выберите папку с проектом:
    `/Users/namazbekbekzhanov/Downloads/DataMining_SIS1_Project-main`
4.  Нажмите **Open**.

## Шаг 2: Настройте окружение (Virtual Environment) 🐍
DataSpell должен предложить настроить интерпретатор, но если нет, сделайте это вручную:

1.  Откройте любой файл с кодом (например, `requirements.txt` или любой `.ipynb`).
2.  В правом нижнем углу окна (или в виджете интерпретатора сверху) нажмите на **<No Interpreter>** или текущую версию Python.
3.  Выберите **Add New Interpreter** -> **Add Local Interpreter...**.
4.  В появившемся окне выберите **Virtualenv Environment** (слева).
5.  Настройки:
    *   **Environment**: Выберите `New`.
    *   **Location**: Оставьте как есть (обычно это папка `venv` внутри проекта).
    *   **Base interpreter**: Убедитесь, что выбран ваш Python 3 (например, `/usr/bin/python3` или тот, что вы установили).
6.  Нажмите **OK**. DataSpell создаст виртуальное окружение.

## Шаг 3: Установите библиотеки (Dependencies) 📦
Теперь нужно установить все необходимые инструменты (pandas, sklearn и т.д.).

1.  Откройте файл `requirements.txt` в редакторе.
2.  DataSpell часто показывает всплывающую панель сверху: **"Package requirements are not satisfied"**.
3.  Нажмите кнопку **Install requirements** на этой панели.
    *   *Альтернативный способ через терминал (внутри DataSpell):*
        1.  Нажмите вкладку **Terminal** (обычно внизу слева).
        2.  Убедитесь, что в начале строки есть `(venv)`.
        3.  Введите команду и нажмите Enter:
            ```bash
            pip install -r requirements.txt
            ```

## Шаг 4: Запустите Notebook 📓
1.  В панели файлов (слева) раскройте папку `notebooks`.
2.  Дважды кликните по файлу `03_modeling.ipynb` (это наш основной файл для SIS 2).
3.  DataSpell может спросить, какому серверу доверять — выберите **Trust Project**.
4.  Убедитесь, что сверху справа выбран ваш новый интерпретатор (venv).
5.  Нажмите кнопку **Run All** (двойная зеленая стрелка `>>` сверху) или запускайте ячейки по очереди, нажимая `Shift + Enter`.

## Готово! 🎉
Теперь вы увидите, как обучаются модели и строятся графики прямо в DataSpell.

---
*Если возникнут ошибки, проверьте, что вы установили все библиотеки из Шага 3.*
